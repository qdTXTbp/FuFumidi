# FuFumidi v3.1.15

## 修复
- **UTAU 自制音库导出**：仅使用录音（未上传音频）时，片段列表与「导出音源」按钮不再被隐藏，可正常命名、微调 oto 并导出完整声库（oto.ini + 各片段 wav）

## 更新方式
- 3.1.x 用户：应用内自动更新（或下载 `FuFumidi.Install.exe` 离线包覆盖安装）
- 新用户：下载 `FuFumidi-Setup-3.1.15.exe` 完整安装
