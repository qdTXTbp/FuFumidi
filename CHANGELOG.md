# 更新日志（Changelog）

> **版本号规范**：大功能更新 → 版本号 **+0.1**（如 1.0.1 → 1.1.0）；缺陷修补 / 小功能更新 → 版本号 **+0.0.1**（如 1.0.1 → 1.0.2）。
> 改版本号必须同步两处：`app/package.json` 的 `version` 与 `app/renderer/FuFumidi.html` 的 `APP_VERSION`。
> 发布流程见 `UPDATE.md`。

---

## 1.2.1 — 2026-08-17（缺陷修补）

### 变更
- **转录队列合并进单转录流程**：取消独立批量模块；同一上传区与「开始转录」按钮支持单个 / 多个 / 整个文件夹，队列列表内联显示。
- **修复批量转录全部失败**：安装包瘦身误删运行期依赖 `mir-eval`，已从卸载清单移除；批量任务现正确携带引擎模式 / 性能档与钢琴踏板参数。

### 修复
- **批量转录「添加文件 / 添加文件夹」无反应**：`bindBatch()` 定义了队列按钮事件但初始化时从未调用；已补上绑定调用，两个按钮现可正常弹出文件 / 目录选择。
- **设置页排版**：引擎页「内置模型」行不再把左侧标签挤压成竖排单字。

## 1.2.0 — 2026-08-15（大功能更新）

### 修复
- **智能修正 NoBackendError**：`smart_midi.py` 原先直接 `librosa.load()` → `audioread`，在未安装 ffmpeg 的机器上抛 `NoBackendError`。改为统一走 `audio_io.load_audio_float32()`（soundfile 快速通道 + 内置 imageio-ffmpeg 静态 ffmpeg），打包环境无需系统 ffmpeg 即可解码 MP3/M4A 等格式；`audio_io` 自身也不再依赖 audioread。

### 修复

- **设置页排版修复**：引擎页「内置模型」行右侧控件 `width:100%` 会把左侧标签挤压成竖排单字列；改为 `flex:1;min-width:0` 且标签列不收缩，模型清单与刷新按钮正常横排。

### 移除
- **移除“风格歌单 · 点击生成”**：删除 6 个程序化生成风格曲目的全部代码与侧边栏入口。

### 新增
- **MIDI 歌单**：替换原资料库。支持导入单个 / 批量 MIDI 文件、递归导入整个文件夹；可新建多个歌单、切换歌单、拖动排序（上移 / 下移）、按名称 / 导入时间排序、收藏与只看收藏；数据存 IndexedDB（不受 localStorage 容量限制），旧 v1.x 资料库自动迁移。
- **编辑器 · 响度调整**：选区 / 整轨力度 ±10%，用于调整响度。
- **编辑器 · 踏板事件**：一键在选区 / 整轨起止添加延音踏板（CC64），或删除区间内踏板事件，导出 MIDI 时 CC 事件完整写回。
- **编辑器 · BPM 调整**：编辑工具栏 BPM 输入 + 应用按钮，改写 MIDI tempo 事件（可撤销），导出后速度不丢失。
- **音色库**：完整 128 个 GM 音色（按 16 个家族分组）+ 新增小提琴、大提琴、竖琴、马林巴、八音盒、颤音琴、合唱、小号、萨克斯、单簧管、双簧管、尼龙 / 钢弦吉他、合成贝斯、西塔琴、日本筝、钟琴、手风琴、班卓琴等内置合成音源。
- **智能配器**：按轨道名称 / 音域 / 复音数 / 音符密度自动选择更合适的 GM 音色；编辑工具栏可对当前轨道智能配器（Shift+点击 = 全部轨道），转录完成后自动执行（Ctrl+Z 可撤销）。
- **按轨 / 全局指定音色**：编辑工具栏音色下拉选择当前轨道，可试听；「全部」把所选音色应用到所有非鼓轨，播放、编辑与 MIDI → WAV 离线渲染即时生效。

### 优化
- **侧边栏布局**：歌单面板限制最大高度并在内部滚动，下方的「工具」区与页脚始终可见；操作按钮常驻显示、点击区域加大。
- **拖动排序**：MIDI 歌单曲目与参数预设管理均改为拖拽排序（⋮⋮ 手柄，上半部插入目标前、下半部插入目标后），移除原来的上移/下移与排序按钮。
- **编辑视图音频波形轨**：载入原音频后，钢琴卷帘底部显示波形峰值与起音竖线（与 MIDI 同一时间轴）；「吸附起音」把选区/整轨音符对齐到最近波形起音（±80ms，可撤销）；「试听」随 MIDI 播放同步回放原音频，智能修正前可先听对齐效果。
- **批量转录队列**：拖入多个音频或选择整个文件夹顺序转录；每任务显示进度/状态，支持暂停、继续、取消全部与失败重试；完成后自动加入歌单。
- **内置 SoundFont 引擎**：内置 FluidSynth WASM 内核（BSD-3 / LGPL-2.1 许可），随应用分发 GeneralUser GS 音源，下拉框直接选择（不再需要外部加载 SF2/SF3）；播放、离线渲染与 WAV 导出全部使用所选音源，可切回内置合成器兜底。
- **模型管理增强**：设置-引擎页模型清单升级为注册表（量化转录 / 钢琴转录 / 人声分离模型状态）；缺失的钢琴转录模型可一键从官方源下载，带实时进度与取消，失败自动清理半成品；主进程注入的离线模型目录保持不变。
- **后台 MP4 导出**：独立浮层选择分辨率 / 帧率 / 时长，录制期间不打断使用；定时器驱动录制并关闭窗口节流，最小化 / 后台也能继续；离屏复用原可视化渲染器，合成为「音符瀑布 + 频谱瀑布 / 示波器 / 垂直琴键」的原布局，按屏幕 DPR 高清输出；H.264 + AAC 与离线音频对齐合成。
- **乐谱增强**：MusicXML 导入转 MIDI；乐谱视图支持五线谱 / 简谱 / 吉他六线谱 / 贝斯四线谱 / 五线谱+简谱对照；导出 PNG；打印排版并导出 PDF。
- **引导修复**：新手引导改为只在首次启动出现一次（设置读取完成后再判断，关闭立即落盘）。

---

## 1.1.0 — 2026-08-14（大功能更新）

### 修复
- **点击资料库 MIDI 却播放内置示例曲**：根因是导入 MIDI 后歌曲队列 App.queue 未随资料库同步重建，点击时索引错位读到旧的内置示例条目；现在 addToLib() 每次导入后重建队列并同步 queueIdx。
- **删除内置示例曲**：移除「夜色列车」演示曲及其侧边栏入口、启动自动载入逻辑与相关文案，无文件时停留在空状态。

### 优化
- **MIDI → WAV 离线渲染大幅提速**：由整曲单图渲染改为按起始时间分桶渲染 + overlap-add 合成，峰值 AudioNode 与缓冲内存下降一个量级，大 MIDI（数万音符）不再卡顿/内存溢出；渲染进度条支持实时百分比。
- **乐谱播放 / 显示优化**：移除逐音符切换的昂贵 drop-shadow 滤镜（消除播放跟随卡顿）、增加渲染缓存（避免切视图 / 缩放反复重绘）、改用精确 BPM（消除谱面与音频的时值漂移错位）。
- **转录显示优化**：转录前显示预计耗时，转录中进度条实时百分比 + 剩余时间，并支持中途取消（终止引擎子进程）。

### 新增
- **编辑器**：选区量化、移调（±半音 / ±八度）、复制 / 粘贴 / 克隆、力度渐强 / 渐弱、音阶吸附。
- **混音台**：每轨音量 / 声像（pan）/ 静音 / 独奏，实时生效。
- **节拍器**：播放时按节拍打点，重拍高音、普通拍低音。
- **工程文件（.fufu）**：保存 / 恢复编辑状态、混音参数、循环区间、播放速度与目标轨道。
- **导出 MusicXML**：把当前曲目导出为标准 MusicXML（part-wise，音符间自动补休止符），可导入 MuseScore / Sibelius 继续编排。
- **实时和弦识别**：演奏视图实时显示当前和弦名（基于正在发声的非鼓轨音符）。
- **循环点拖拽**：演奏视图 Shift+拖拽设置循环区间，并在卷帘中高亮循环区域。
- **歌词显示**：解析 MIDI 歌词轨道，播放时逐行高亮（卡拉 OK 式）。

### 界面
- **安装 / 卸载界面**：优化字体显示与页面设计（详见 build/installer.nsh）。

---

## 1.0.2 — 2026-08-14（缺陷修补）

### 修复
- **MIDI 转换后无法导出保存 WAV 音频文件**：根因是 Electron 33（Chromium 130）下 `<a download>` 锚点下载经 `will-download` 的 `preventDefault + setSavePath + resume` 处理后被 0 字节取消（blob/data/http 均复现）。改为 IPC `file:saveBinary`：渲染进程把字节交给主进程，走原生「保存文件」对话框并 `fs` 写盘，彻底绕开失效的下载子系统。
- **编辑视图「导出 MIDI」同样失效**：同一锚点下载机制，一并切换到 `file:saveBinary`。
- **「打开输出文件夹」定位错误**：此前转换结果只默认落在下载目录，「打开输出文件夹」始终打开该目录，与用户实际选择的保存位置脱节；现在保存后记住实际路径（`convSavedPath`），该按钮直接定位到刚保存的文件。

---

## 1.0.1 — 2026-08-13（修补 + 小功能）

### 优化
- 内置 Python 运行时瘦身：新增 `scripts/prune-python.js`，删除全部 `__pycache__`、标准库测试套件、torch 构建工具目录；`bundle-python.js` 构建后自动调用。安装包由 **853MB → 556MB**（-35%）。

### 新增
- **内置两个推荐预设并设为默认**：
  - 「人声：最优」（人声分离模式）：起音 0.05 / 判定 0.25 / 最短音符 100ms / 输出鼓组节奏轨 / 智能降噪 / 响度平衡 / 自动 BPM。
  - 「钢琴：最优」（钢琴专用模式）：起音 0.05 / 判定 0.06 / 最短音符 20ms / 合并间隔 0ms / 含踏板 / 智能降噪 / 响度平衡。
- 新装默认应用「人声：最优」；手动切换引擎模式时自动套用该模式的默认预设。
- 转录高级参数新增两项：
  - 钢琴模式「音符合并间隔」滑杆（0–200ms，对应引擎 `merge_gap_ms`）。
  - 人声分离模式「输出鼓组节奏轨」开关（对应引擎 `with_drums`）。
- 「最短音符」滑杆范围放宽至 10–300ms（配合钢琴预设的 20ms）。
- **预设管理**：可对预设列表 ↑/↓ 排序（持久化到 `presets.json` 的 `order`）、删除内置预设（隐藏机制 + 「恢复全部内置预设」一键还原）、点击名称立即应用；删除操作改为二次确认防误删。

### 修复
- 设置 / 主题库对话框右上角「×」关闭按钮失效：`data-close` 存的 id 缺 `#` 前缀，`querySelector` 取到 null 抛 TypeError。
- 卸载后残留空安装目录：卸载器 CWD 位于 `$INSTDIR` 时 Windows 锁定当前目录，`RMDir` 删不掉目录本身；`customRemoveFiles` 先 `SetOutPath "$TEMP"` 再删。

---

## 1.0.0 — 2026-08（大版本）

- 七个视图一体：演奏 / 编辑 / 可视化 / 分析 / 乐谱 / 转录 / 转换。
- 三引擎离线转录（basic-pitch / piano-transcription / demucs）+ 智能修正（refine）。
- 内置 Python 运行时开箱即用；GPU 自动检测与三档性能模式。
- 主题库 / 插件系统 / 新手引导 / 完整性一键修复 / 中英双语界面。
